import React from "react";

/**
 * Handle the google map marker visualisation and the popover
 */
class Marker extends React.Component {

  constructor(props) {
    super(props);
    this.handleHover = this.handleHover.bind(this);
  }

  handleHover(res) {

  }

  render() {
    return (
      <React.Fragment>

      </React.Fragment>
    )
  }
}

export default GeolocInput;